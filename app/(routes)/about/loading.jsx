import { tailwindPadding } from '@/app/_data/constants'
import React from 'react'

const loading = () => {
  return (
    <div className={`${tailwindPadding}`}>loading..</div>
  )
}

export default loading